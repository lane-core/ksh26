ref	-D_def_map_ast=1
iff	SFSTDIO
cat{
	/*
	 * ast_stdio.h — minimal stub
	 *
	 * The sfio interception layer that previously remapped all stdio
	 * symbols to _ast_* variants has been retired (sfio retirement).
	 * This header is kept for build compatibility but defines nothing.
	 * Real stdio comes from <stdio.h> via the system header.
	 */
}end
