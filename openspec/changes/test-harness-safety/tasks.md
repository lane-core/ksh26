## 1. Create safety.sh

- [x] 1.1 Create `tests/lib/safety.sh` with ISC attribution header
- [x] 1.2 Implement `die` — SIGPIPE to $$ for subshell escape, include $LINENO if available
- [x] 1.3 Implement `harden` — wrap commands with die-on-failure
- [x] 1.4 Implement `extern` — bypass builtins/functions, walk PATH for real binary
- [x] 1.5 Implement `pushtrap` / `poptrap` — stack-based trap management
- [x] 1.6 Implement safe mode — `set -o nounset`, `set -o noglob` for harness code
- [x] 1.7 Detect and enable `pipefail` when available (ksh, bash — not dash)

## 2. Integrate into test harness

- [x] 2.1 Source `safety.sh` from `run-test.sh` (generated by configure.sh)
- [x] 2.2 Harden harness commands: mkdir, cp, rm, date, dirname, cat, mktemp
- [x] 2.3 Convert `run-test.sh` manual trap to `pushtrap`
- [ ] 2.4 Update `tests/contexts/default.sh` to use `pushtrap` for its cleanup (N/A — no traps in default.sh currently)

## 3. Validation

- [x] 3.1 Darwin: `just test` — 102/102 gate, 8/8 advisory
- [x] 3.2 Linux: `just test-linux` — 102/102 gate, 8/8 advisory
- [ ] 3.3 Provoke a harness command failure — verify die fires with diagnostic including line number
- [ ] 3.4 Verify nested traps execute in LIFO order on exit
- [ ] 3.5 Verify `extern mkdir` calls the PATH binary, not a function
- [ ] 3.6 Verify `pipefail` is active on ksh/bash, absent on dash (no error)
