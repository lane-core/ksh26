## Phase -1: Probe hygiene audit

- [x] -1.1 For every define generated by the monolith's 56 probes, grep `src/**/*.{c,h}` for consumers. Produce a table: probe → defines → consumer files (or "none").
- [x] -1.2 Classify each probe: **keep** (has consumers), **simplify** (C23-guaranteed, make unconditional), **eliminate** (no consumers).
- [x] -1.3 Document dead platform branches in `probe_ast_standards` — which platforms does ksh26 actually target?
- [x] -1.4 Fix the `_mmap_worthy` bug: emit `#define _mmap_worthy 2` when performance test passes. Build+test passes (100/102).
- [x] -1.5 Write `notes/configure-audit.md` with the full audit report: kept probes, simplified probes, eliminated probes, platform scope decision, bugs found.
- [x] -1.6 Document expected manifest probe count: 54 probes (4 eliminated from original 58). Full table in notes/configure-audit.md §Manifest probe list.
- [x] -1.7 **Gate: audit reviewed and approved before any implementation begins.**

## Phase 0: Foundation

- [x] 0.1 Vendor modernish bundle into `build/lib/modernish/` via `install.sh -B`
- [x] 0.2 Create directory structure: `build/configure/`, `build/probes/`, `build/probes/data/`
- [x] 0.3 Write `configure.sh` orchestrator (~75 lines): source modernish, source modules, run phases
- [x] 0.4 Write `build/configure/driver.sh`: `choose`, `trylibs`, `hdr`/`lib`/`mem`/`typ`/`dat` batch helpers, `try_variants`, sysdeps recording, atomic_write, probe primitives
- [x] 0.5 Write `build/configure/manifest.sh`: probe registry with name, tier, deps, type, output mapping for all 54 probes. Wired into run_probes.
- [x] 0.6 Write `build/configure/gen-features.sh`: copy_features (FEATURE→header mapping from manifest), fixup_ast_common, install_std_wrappers, install_endian_stubs. Wired into run_generators.
- [x] 0.7 **Gate: framework runs with zero probes. modernish initializes, driver loads, empty sysdeps produced. Tested on darwin.arm64-64.**

## Phase 1: Standalone C probes (Category A+B — 29 probes)

- [ ] 1.1 Extract Category A C programs to `build/probes/try_*.c` (18 probes)
- [ ] 1.2 Write batch probe calls in manifest for simple hdr/lib/mem/typ/dat within Category A probes
- [ ] 1.3 Extract Category B C programs to `build/probes/try_*.c` (11 probes)
- [ ] 1.4 Integrate `choose` driver calls for all 29 probes
- [ ] 1.5 Extract verbatim text blocks to `build/probes/data/*.cat`
- [ ] 1.6 **Gate: diff sysdeps output against monolith's FEATURE headers for these 29 probes. `just build` + `just test`.**

## Phase 2: Complex probes (Category C — 17 probes)

- [ ] 2.1 Port `probe_ast_standards` — u_long + 10-platform cascade. Extract 10 try_standards_*.c files + data/*.cat output blocks.
- [ ] 2.2 Port `probe_ast_common` — integer sizing, va_list (try_variants), __func__. Extract try_*.c files.
- [ ] 2.3 Port `probe_ast_lib` — batch + 3 complex sub-probes (spawnveg output, wchar execute, posix_spawn).
- [ ] 2.4 Port `probe_ast_float` — largest single probe (600 lines). Extract FLOATSRC/DBLSRC/FMAXSRC to try_*.c files.
- [ ] 2.5 Port remaining 13 Category C probes (fs, sys, wchar, tty, hack, nl_types, iconv, stdio, dirent, tmlib, mmap, tvlib, externs).
- [ ] 2.6 **Gate: diff ALL FEATURE headers against monolith. `just build` + `just test`.**

## Phase 3: Shell probes + generators (Category D — 14 probes/generators)

- [ ] 3.1 Port shell probes: locale (`locale -a`), cmds (`[ -x ... ]`), siglist (`kill -l`)
- [ ] 3.2 Port delegated probes: sig.sh, param.sh, libpath.sh (already separate files — wire into driver)
- [ ] 3.3 Write `gen-math.sh`: read sysdeps + math.tab → FEATURE/math + shtab_math[]
- [ ] 3.4 Write `gen-shopt.sh`: SHOPT.sh → shopt.h
- [ ] 3.5 Write `gen-headers.sh`: git.h, cmdext.h, cmdlist.h, ast_release.h
- [ ] 3.6 Write `gen-conf.sh`: conf.sh + lcgen → conflim.h/conftab.h/lc.h
- [ ] 3.7 Port static probes: api (pure text), eaccess stub, fchdir stub
- [ ] 3.8 **Gate: diff ALL FEATURE headers + generated files against monolith. `just build` + `just test`.**

## Phase 4: Emitters (non-probe output)

- [ ] 4.1 Port `ninja.sh`: emit_ninja (~240 lines)
- [ ] 4.2 Port `sources.sh`: collect_{libast,libcmd,ksh26}_sources
- [ ] 4.3 Port `test-infra.sh`: generate_test_env + embedded run-test
- [ ] 4.4 Wire core.sh: options, HOSTTYPE detection, paths, C23 gate, cache, dirs
- [ ] 4.5 **Gate: full `just build` + `just test` with new system end-to-end. Monolith no longer needed.**

## Phase 5: Verification + polish

- [ ] 5.1 Add `--with-sysdep-K=V` manual override mechanism (skalibs pattern)
- [ ] 5.2 Add input-hash comment to FEATURE headers via `gen-features.sh`
- [ ] 5.3 Verify: two consecutive `--force` runs produce identical sysdeps + FEATURE headers
- [ ] 5.4 Document module dependencies in each file's header comment
- [ ] 5.5 **Final gate: `just test` darwin + `just test-linux`**
- [ ] 5.6 Diff all 56 FEATURE headers against monolith (final oracle check)
- [ ] 5.7 Delete monolith code from configure.sh (preserve on replace-iffe-v2 branch)
